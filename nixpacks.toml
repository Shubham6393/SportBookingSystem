[phases.setup]
nixpkgs = "nixos-23.11"

[phases.install]
nixpkgsPackages = ["php83", "composer"]

[phases.build]
cmds = ["composer install"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
